node_modules/
dist/
*.log
.env
.env.local

# src/component/_generated is deliberately NOT ignored. Convex codegen needs a
# linked deployment to run, so committing its output is what lets the package
# build (and publish) from a clean checkout without one.
